# Configuração do banco de dados PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5432/clinicaapi2
# spring.datasource.username=postgres
# spring.datasource.password=jonas1234

# # Configuração JPA
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update

# # Configuração CORS
# spring.webmvc.cors.allowed-origins=http://127.0.0.1:5500
# spring.webmvc.cors.allowed-methods=GET,POST,PUT,DELETE
# spring.webmvc.cors.allowed-headers=*

# PostgreSQL
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}  
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.operationsSorter=method